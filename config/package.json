{
  "name": "setup",
  "version": "0.0.2",
  "description": "setup directus, build and deploy",
  "main": "index.js",
  "author": "jonathan attar",
  "license": "MIT",
  "private": true,
  "scripts": {
    "setup": "node ./setup",
    "build": "node build.js --app your-app-id",
    "deploy": "node deploy.js --app"
  },
  "dependencies": {
    "@directus/sdk-js": "^6.0.1",
    "zip-dir": "^1.0.2"
  }
}
